# Cota Madrid

Premium landing site for Cota — a Madrid reform business.

Built with Next.js, TypeScript, Tailwind v4, and Vitest. Full README to follow.

## Scripts

- `npm run dev` — start dev server
- `npm run build` — production build
- `npm test` — run Vitest tests
- `npm run lint` — lint
